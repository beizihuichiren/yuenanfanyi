---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: b49a8f511d795a7d907eefcb960f8390_745a72e2913b11f1bafa525400287e28
    ReservedCode1: 2/z8pA1fMV3qx1kRolevi/Rd5T8UFtpTciH5hdq1f0n0n+FfFSdjOINTUF0PssXoiwaulT5MWNu5pgYmuGto8fPvXVRm8WNFjpwOkTlEDHQmaJbmTkwI5JFC86KmwgC7HWA63vz9SSlRFrSoyESAVYdBz4kMm62BZrw1orBFsnWfuLUE/EU3AqTwJCE=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: b49a8f511d795a7d907eefcb960f8390_745a72e2913b11f1bafa525400287e28
    ReservedCode2: 2/z8pA1fMV3qx1kRolevi/Rd5T8UFtpTciH5hdq1f0n0n+FfFSdjOINTUF0PssXoiwaulT5MWNu5pgYmuGto8fPvXVRm8WNFjpwOkTlEDHQmaJbmTkwI5JFC86KmwgC7HWA63vz9SSlRFrSoyESAVYdBz4kMm62BZrw1orBFsnWfuLUE/EU3AqTwJCE=
---

# 短剧越南语转译自动化工程方案

## 一、项目概述

将短剧中文字幕自动提取、翻译为越南语，经人工审核后消除原字幕、烧录越南语字幕并质检导出。目标：**除越南语审核外，全流程脚本化，零 GUI 依赖**。

## 二、技术架构

```
┌─────────────────────────────────────────────────────────┐
│                      调度层 (CLI)                        │
│                  pipeline.py <剧名> <集数范围>             │
├─────────────────────────────────────────────────────────┤
│  模块层                                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ download │ │ extract  │ │translate │ │  review   │   │
│  │  下载模块 │ │ 字幕提取  │ │  翻译模块 │ │ 审核模块   │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐               │
│  │ remove   │ │  merge   │ │  quality │               │
│  │ 消字幕模块│ │ 合成模块  │ │ 质检模块  │               │
│  └──────────┘ └──────────┘ └──────────┘               │
├─────────────────────────────────────────────────────────┤
│  基础设施层                                              │
│  ┌──────┐ ┌──────┐ ┌───────┐ ┌──────┐ ┌───────────┐  │
│  │aria2 │ │Whisper│ │OpenAI │ │FFmpeg│ │vsr(Docker)│  │
│  │      │ │      │ │API    │ │      │ │           │  │
│  └──────┘ └──────┘ └───────┘ └──────┘ └───────────┘  │
└─────────────────────────────────────────────────────────┘
```

## 三、环境要求

| 组件 | 版本/规格 | 用途 |
|------|----------|------|
| Python | 3.12+ | 主调度 + 脚本 |
| FFmpeg | 5.0+ | 烧录字幕 / 画面质检 |
| aria2 | 1.36+ | 夸克直链多线程下载 |
| Docker | 24+ | video-subtitle-remover 容器 |
| NVIDIA GPU | 显存 ≥ 8GB | Whisper + 消字幕（CPU 可降级，慢 5-10x） |
| CUDA | 12.4+ | GPU 加速 |

## 四、目录结构

```
short-drama-pipeline/
├── pipeline.py              # 主调度脚本（统一入口）
├── config.yaml              # 全局配置
├── requirements.txt
│
├── modules/
│   ├── downloader.py        # 夸克直链解析 + aria2 下载
│   ├── extractor.py         # Whisper 语音识别提取 SRT
│   ├── translator.py        # OpenAI/DeepSeek API 批量翻译
│   ├── review.py            # 中越双语审核 DOCX 生成 + 审核意见应用
│   ├── remover.py           # video-subtitle-remover Docker 调用
│   ├── merger.py            # FFmpeg 字幕烧录
│   └── quality.py           # 画面完整性质检（黑屏/花屏/卡顿/黑边）
│
├── utils/
│   ├── srt_parser.py        # SRT 解析/校验/比对
│   ├── srt_crosscheck.py    # 剪映+Whisper 双路交叉校验
│   └── docx_utils.py        # Word 表格生成
│
├── prompts/
│   ├── translate.txt        # 翻译 prompt 模板
│   ├── gen_review.txt       # 生成审核文档 prompt 模板
│   └── apply_review.txt     # 应用审核意见 prompt 模板
│
├── docker/
│   └── docker-compose.yml   # video-subtitle-remover 容器
│
└── data/
    └── glossary.yaml        # 人名/地名/专有名词术语表
```

## 五、配置文件

```yaml
# config.yaml

# --- 项目路径 ---
workspace: /data/short-drama
download_dir: ${workspace}/downloads
output_dir: ${workspace}/output

# --- API 配置 ---
api:
  provider: openai          # openai | deepseek | custom
  base_url: https://api.openai.com/v1
  api_key: ${OPENAI_API_KEY}
  model: gpt-4o
  temperature: 0.3
  max_tokens: 4096

# --- Whisper 配置 ---
whisper:
  model: large-v3           # tiny | base | small | medium | large-v3
  device: cuda
  language: zh
  beam_size: 5
  vad_filter: true          # 过滤静音段

# --- 夸克下载配置 ---
quark:
  cookie: ${QUARK_COOKIE}   # 夸克登录 Cookie
  user_agent: "Mozilla/5.0 ..."
  max_concurrent: 3         # 并发下载数

# --- 消字幕配置 ---
subtitle_remover:
  image: eritpchy/video-subtitle-remover:1.1.1-cuda12.8
  algorithm: sttn           # sttn | lama | propainter
  gpu_device: 0

# --- 字幕样式 ---
subtitle_style:
  font_name: Arial
  font_size: 20
  primary_color: "&H00FFFFFF"     # 白色
  outline_color: "&H00000000"     # 黑色描边
  outline: 2
  alignment: 2                    # 底部居中

# --- 质检阈值 ---
quality:
  black_detect_duration: 1.0      # 黑屏检测时长阈值(秒)
  black_detect_threshold: 0.10    # 黑屏像素亮度阈值
  freeze_detect_duration: 2.0     # 静帧检测时长阈值(秒)
  glitch_scene_threshold: 0.8     # 花屏检测场景变换阈值
  edge_sample_frames: 10          # 黑边检测采样帧数
```

## 六、模块设计

### 6.1 下载模块 (`downloader.py`)

**功能**：从夸克网盘分享链接解析直链 → aria2 下载。

**流程**：
1. 读取片源表格（金山文档 CSV 导出），获取分享链接列表
2. 调用夸克 API 解析分享链接 → 提取 file_id
3. 用 Cookie 换取直链 → 喂给 aria2 RPC
4. 校验文件完整性（MD5 / 文件大小对比）
5. 按剧集命名：`{剧名}_EP{集数}_原始片源.mp4`

**关键依赖**：`requests`, `aria2p`（aria2 JSON-RPC 客户端）

### 6.2 字幕提取模块 (`extractor.py`)

**功能**：Whisper 语音识别 → 中文 SRT。

**流程**：
1. 用 `faster-whisper` 加载模型
2. 提取音频 → VAD 切片 → 逐段识别 → 合并
3. 后处理：去重相邻重复句、合并过短短语
4. 输出 SRT（UTF-8，序号连续，时间轴递增）

**备注**：如需剪映双路校验，额外调用 `srt_crosscheck.py`，用编辑距离 + 规则生成差异报告，人工只看红色条目。

### 6.3 翻译模块 (`translator.py`)

**功能**：中文 SRT → 越南语 SRT，保留原始结构和时间轴。

**流程**：
1. 解析 SRT 为 `[{index, start, end, text}]`
2. 加载术语表（人名/地名/专有名词），注入 system prompt
3. 分批提交 API（每批 30-50 条，带序号上下文）
4. 校验输出：条目数一致、序号连续、时间轴不变
5. 输出 `{剧名}_EP{集数}_越南语字幕_初译版.srt`

**Prompt 核心约束**：
```
你是专业中越字幕翻译。严格规则：
1. 只替换字幕文字，禁止修改序号、时间轴、条目数量
2. 输出标准 SRT 格式，每块用空行分隔
3. 禁止合并、拆分、删除或新增任何字幕条目
4. 使用越南南部日常口语，自然流畅
```

### 6.4 审核模块 (`review.py`)

#### 6.4.1 生成审核文档

**功能**：中文 SRT + 越南语初译 SRT → 中越双语审核 DOCX。

**流程**：
1. 解析两份 SRT，按序号对齐
2. 用 `python-docx` 生成 Word 表格：

| 字幕序号 | 时间轴 | 中文字幕 | 越南语初译 | 审核意见 | 修改建议 |
|---------|--------|---------|-----------|---------|---------|
| 1 | 00:00:05,000 --> 00:00:08,500 | 你好吗 | Bạn có khỏe không | | |

3. 输出 `{剧名}_EP{集数}_中越双语字幕审核稿.docx`

#### 6.4.2 应用审核意见

**功能**：审核 DOCX + 初译 SRT → 终版越南语 SRT。

**流程**：
1. 解析审核 DOCX，提取有 "审核意见" 或 "修改建议" 的条目
2. 将初译 SRT + 审核条目一起送 API
3. API 逐条落实修改，并自动处理同类问题（如人名统一）
4. 校验输出完整性
5. 输出 `{剧名}_EP{集数}_越南语字幕_审核终版.srt`

### 6.5 消字幕模块 (`remover.py`)

**功能**：本地 AI 去除硬字幕，替代开拍网站。

**实现**：
```python
def remove_subtitle(input_video: str, output_video: str, algorithm="sttn"):
    """调用 video-subtitle-remover Docker 容器"""
    cmd = [
        "docker", "run", "--rm",
        "--gpus", f"device={config.gpu_device}",
        "-v", f"{Path(input_video).parent}:/data",
        config.subtitle_remover.image,
        "-i", f"/data/{Path(input_video).name}",
        "-o", f"/data/{Path(output_video).name}",
        "-a", algorithm,
    ]
    subprocess.run(cmd, check=True)
```

**算法选择**：
- 短剧（真人为主）→ `sttn`（速度与效果最佳平衡）
- 动画类短剧 → `lama`
- 运动镜头多的动作短剧 → `propainter`

### 6.6 合成模块 (`merger.py`)

**功能**：消字幕视频 + 终版越南语 SRT → 成片。

```bash
ffmpeg -i 消除字幕版.mp4 \
  -vf "subtitles=终版越南语.srt:force_style=\
'FontName=Arial,\
FontSize=20,\
PrimaryColour=&H00FFFFFF,\
OutlineColour=&H00000000,\
Outline=2,\
Alignment=2'" \
  -c:a copy \
  -c:v libx264 -crf 18 -preset medium \
  成片.mp4
```

### 6.7 质检模块 (`quality.py`)

**功能**：自动检测画面问题，生成质检报告。

**检测项**：

| 检测项 | 方法 | 输出 |
|--------|------|------|
| 黑屏 | `ffmpeg blackdetect` | 时间区间列表 |
| 静帧/卡顿 | `ffmpeg freezedetect` | 时间区间列表 |
| 花屏嫌疑 | 帧间 PSNR 突变分析 | 时间戳 + 截图 |
| 开头/结尾不完整 | 前/后 N 帧纯色检测 | 布尔值 |
| 画面裁切/黑边 | 边缘像素采样 | 黑边位置 |

输出：`{剧名}_EP{集数}_质检报告.md`，包含异常时间戳和关键帧截图。

## 七、主调度脚本

```python
# pipeline.py

import argparse
from modules import downloader, extractor, translator, review, remover, merger, quality

def run_episode(name: str, episode: int):
    ep = f"{name}_EP{episode:02d}"

    # Phase 1: 下载
    raw = downloader.download(name, episode)
    # → ep_原始片源.mp4

    # Phase 2: 提取字幕 + 交叉校验
    cn_srt = extractor.extract(raw)
    # → ep_中文字幕.srt
    extractor.crosscheck(raw, cn_srt)
    # → ep_字幕校验报告.html

    # Phase 3: 翻译
    vi_srt_v1 = translator.translate(cn_srt, target="vi")
    # → ep_越南语字幕_初译版.srt

    # Phase 4: 生成审核文档 → [人工审核] → 应用修改
    review_docx = review.generate(cn_srt, vi_srt_v1)
    print(f"  → 审核文档: {review_docx}")
    print(f"  → 请人工审核后保存到: {review_docx}")
    input("  → 审核完成后按回车继续...")
    vi_srt_final = review.apply_review(vi_srt_v1, review_docx)
    # → ep_越南语字幕_审核终版.srt

    # Phase 5: 消字幕
    clean = remover.remove(raw)
    # → ep_消除字幕版.mp4

    # Phase 6: 合成
    final = merger.merge(clean, vi_srt_final)
    # → ep_越南语转译成片.mp4

    # Phase 7: 质检
    quality.check(final)
    # → ep_质检报告.md

    print(f"✅ {ep} 完成")

if __name__ == "__main__":
    parser = argparse.ArgumentParser()
    parser.add_argument("name", help="短剧名称")
    parser.add_argument("--start", type=int, default=1)
    parser.add_argument("--end", type=int, required=True)
    args = parser.parse_args()

    for ep in range(args.start, args.end + 1):
        run_episode(args.name, ep)
```

**使用方式**：
```bash
python pipeline.py "霸道总裁爱上我" --start 1 --end 20
```

## 八、部署步骤

### 8.1 基础环境

```bash
# 系统依赖
apt install ffmpeg aria2 docker.io nvidia-container-toolkit

# Python 环境
git clone <repo>
cd short-drama-pipeline
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
```

### 8.2 拉取消字幕镜像

```bash
docker pull eritpchy/video-subtitle-remover:1.1.1-cuda12.8
```

### 8.3 下载 Whisper 模型

```bash
# 首次运行自动下载，或手动预置
python -c "from faster_whisper import WhisperModel; WhisperModel('large-v3')"
```

### 8.4 配置环境变量

```bash
export OPENAI_API_KEY="sk-xxx"
export QUARK_COOKIE="xxx"
```

### 8.5 验证

```bash
python pipeline.py "测试剧" --start 1 --end 1
```

## 九、交付文件清单

每集最终产出：

```
{剧名}/
├── EP01_原始片源.mp4
├── EP02_中文字幕.srt
├── EP03_越南语字幕初译.SRT
├── EP04_中越双语字幕审核意见
├── EP05_越南语审核意见
├── EP06_越南语审核终版.srt
├── EP07_开拍消除字幕视频.mp4
├── EP08_越南语转译成片.mp4
├── EP09_质检报告.md
└── EP01_质检截图/
    ├── black_00.02.13.png
    └── ...
```

## 十、注意事项

1. **夸克 Cookie 有效期**：约 7-30 天，过期需手动刷新。可对接通知告警。
2. **Whisper 准确率**：large-v3 对中文识别准确率约 95%，建议保留交叉校验环节。
3. **API 费用**：每集 10 分钟短剧约消耗 15K-25K tokens（翻译 + 审核文档生成 + 修改），GPT-4o 约 $0.1-0.2/集。
4. **消字幕质量**：STTN 对固定位置字幕效果好，若字幕位置频繁变动（如综艺），考虑 propainter 或手动标注区域。
5. **GPU 资源**：消字幕 + Whisper 同时跑需要约 12GB 显存，显存不足时分步串行执行。
6. **文件命名**：严格按 `{剧名}_EP{集数}_{用途}.{扩展名}` 规范，脚本依赖此命名做文件匹配。
*（内容由AI生成，仅供参考）*
